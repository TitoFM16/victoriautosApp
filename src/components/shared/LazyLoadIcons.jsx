import { useEffect } from "react";

const LazyLoadIcons = () => {
  useEffect(() => {
    // Material Icons are loaded via CSS, so we don't need any special loading logic
  }, []);

  return null;
};

export default LazyLoadIcons;
